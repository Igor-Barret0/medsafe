name: medsafe
description: "Medisafe - Seu cuidado, no horário certo"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^14.2.0
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  email_validator: ^3.0.0
  intl: ^0.19.0
  mask_text_input_formatter: ^2.9.0
  file_picker: ^8.1.2
  package_info_plus: ^8.1.0
  device_info_plus: ^10.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
